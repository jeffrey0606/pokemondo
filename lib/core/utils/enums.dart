enum AppThemeMode {
  light,
  dark,
  system,
}

enum DialogType {
  theme,
  lang,
}

enum AppTabs { allPokemons, favourites }

enum ImageType {
  asset,
  network,
}

enum FavouritesStatus {
  initial,
  loading,
  loaded,
  failed,
}

enum FavouritesActionStatus {
  adding,
  removing,
  inactive,
  failed,
}


enum PaginationStatus {
  initial,
  loading,
  loaded,
  failed,
}

enum ToastMessageType{
  error,
  info,
  success,
}